# --- Java (Toptal) ---
*.class
*.log
*.ctxt
.mtj.tmp/
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar
hs_err_pid*
replay_pid*

# --- IDE / build (keep if useful) ---
target/
build/
.idea/
*.iml
.vscode/

# --- MinIO runtime data (do not version) ---
elk/minio_storage/**        # your actual path
**/.minio.sys/**            # MinIO system dir anywhere
*.meta                      # xl.meta etc.

# --- Elasticsearch data (if bind-mounted) ---
elk/elasticsearch/data/**

# --- Logstash runtime & sample inputs (optional) ---
logstash/data/**
logstash/logstash-ingest-data/**
logstash/.sincedb*

# --- OS junk & secrets ---
Thumbs.db
.DS_Store
.env

# --- If these PDFs are uploads, not code ---
pdfs/**
